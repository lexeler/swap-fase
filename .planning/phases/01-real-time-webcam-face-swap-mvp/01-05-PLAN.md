---
phase: 01-real-time-webcam-face-swap-mvp
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - src/swapfase/app.py
  - src/swapfase/capture.py
  - src/swapfase/engine.py
  - src/swapfase/ui/main_window.py
  - README.md
autonomous: false
requirements: [LIVE-03, SWAP-03, UI-05]
must_haves:
  truths:
    - "When the GPU is unavailable the app degrades to a working CPU fallback through the same pipeline (slower but functional, no crash) and the badge shows CPU"
    - "The GPU/CPU badge shows the REAL active provider and the FPS reflects the actual loop speed"
    - "A frame with no detected face passes through unchanged without crashing (verified live)"
    - "Camera busy / not found surfaces a friendly message (not a stack trace); a photo with no face surfaces a friendly message and keeps the previous target"
    - "The camera is always released on Stop and on crash/close (re-launch always reopens it)"
  artifacts:
    - path: "src/swapfase/app.py"
      provides: "CPU-fallback wiring through the same pipeline + friendly error surfacing + guaranteed release on stop/crash"
      contains: "CPUExecutionProvider"
    - path: "README.md"
      provides: "Run instructions (run.sh), the GPU-fix note, model acquisition, and the personal-use/offline note"
      contains: "run.sh"
  key_links:
    - from: "src/swapfase/engine.py / providers.py"
      to: "src/swapfase/ui/main_window.py badge"
      via: "real active provider string surfaced to the GPU/CPU badge"
      pattern: "provider"
    - from: "src/swapfase/capture.py (busy/not-found)"
      to: "src/swapfase/app.py error dialog"
      via: "catchable capture exception → friendly QMessageBox"
      pattern: "QMessageBox"
---

<objective>
Make the app robust and trustworthy. Wire the CPU graceful-fallback path through the SAME pipeline so that if CUDA is unavailable the app still runs (slower but functional, no crash) with the badge honestly showing "CPU fallback" (LIVE-03, D-17). Surface the REAL active provider and actual FPS. Harden the error surfaces: camera busy / not found and no-face-in-photo become friendly messages, not stack traces; the camera is ALWAYS released on Stop and on crash/close. Verify the no-face passthrough live. Finish with a README and a final human verification of the whole experience.

This is the robustness/degradation vertical slice on top of the working app — it changes no architecture, only fail-safe behavior and honest status.

Purpose: Step 8 of the ROADMAP risk-first build order — graceful-degradation polish. Closes the remaining success criteria (CPU survival, no-face passthrough live, friendly errors) and the "Looks Done But Isn't" checklist.
Output: CPU-fallback wiring + friendly error handling + guaranteed device release, a README, and a final human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-real-time-webcam-face-swap-mvp/01-CONTEXT.md
@.planning/phases/01-real-time-webcam-face-swap-mvp/01-SKELETON.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md

<interfaces>
<!-- From Plans 01–04 — use directly: -->
def select_providers(prefer_gpu: bool = True) -> list[str]   # already returns CPU-only when CUDA absent (D-17)
def verify_gpu(raise_on_cpu: bool = False) -> str
class FaceEngine: provider: str; using_gpu: bool
class NoFaceError(Exception): ...
class AppState: provider: str; fps: float; ...
class CaptureThread: def __init__(self, device_index, buffer, state); def stop(self)
class MainWindow(QMainWindow): frame_ready = Signal(object); def on_frame(self, frame)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CPU graceful fallback through the same pipeline + honest provider/FPS badge + friendly errors + guaranteed release</name>
  <read_first>
    - .planning/research/PITFALLS.md (Pitfall 3 visibility; Pitfall 10 release on crash; UX Pitfalls: CPU-fallback indicator, camera-busy message; "Looks Done But Isn't" checklist; Security/Privacy table)
    - .planning/research/ARCHITECTURE.md (Pattern 2 graceful degradation; Scaling: CPU fallback through same pipeline)
    - .planning/phases/01-real-time-webcam-face-swap-mvp/01-CONTEXT.md (D-17 GPU→CPU fallback, D-18 no-face passthrough, UI-05 real provider badge, SWAP-03)
    - src/swapfase/app.py, src/swapfase/capture.py, src/swapfase/engine.py, src/swapfase/ui/main_window.py (the files being modified)
  </read_first>
  <files>src/swapfase/app.py, src/swapfase/capture.py, src/swapfase/engine.py, src/swapfase/ui/main_window.py</files>
  <action>
    Finalize graceful degradation and honest status across the existing app (no new pipeline):
    - **CPU fallback through the same pipeline (LIVE-03, D-17):** ensure bootstrap/`app.py` never hard-requires GPU — if `select_providers(prefer_gpu=True)` yields CPU-only (CUDA absent), `FaceEngine` builds on `CPUExecutionProvider` with `ctx_id=-1` and the SAME pipeline runs (slower, still functional, no crash). The keep-newest buffer means CPU shows fewer/older-by-one frames but never a growing backlog. Do NOT crash or exit when GPU is missing. (verify_gpu is informational here — raise_on_cpu MUST be False in the app path; the hard gate is the dev probe from Plan 01.)
    - **Honest badge (UI-05):** the GPU/CPU badge shows `engine.provider` (the REAL bound provider from a warm-up inference — Pitfall 3), not what was requested. The FPS label shows the actual measured loop fps. If on CPU, the badge clearly reads "CPU fallback" so the user understands the slow path (UX Pitfall).
    - **Friendly errors (no stack traces):**
      - Camera busy / not found: `capture.py` raises a clear catchable exception ("Device or resource busy" / no capturable node); `app.py` catches it and shows a `QMessageBox` like "Camera in use or not found — close other apps using the webcam (Zoom/Meet/browser) and try again." (Pitfall 10).
      - No face in the loaded photo: already caught in Plan 04's load handler — confirm it shows a friendly `QMessageBox` and keeps the previous target (SWAP-01 robustness).
    - **No-face passthrough confirmed live (SWAP-03, D-18):** ensure `engine.process` returns the frame unchanged when no face is detected and the inference loop never crashes on a faceless frame.
    - **Always release the device (Pitfall 10):** confirm `cap.release()` runs on Stop, on device-switch, on window close (`closeEvent`), AND on an unhandled capture-loop exception (try/finally). Re-launching the app must always reopen the camera.
    - **Privacy/offline (Security table):** confirm no network call exists in the frame loop and no frames are written to disk (the only network is the one-time model download in `ensure_models`).
  </action>
  <verify>
    <automated>cd /home/lexeler/swap-fase && grep -q "CPUExecutionProvider" src/swapfase/app.py && grep -qi "QMessageBox" src/swapfase/app.py && grep -qi "release" src/swapfase/app.py && grep -qi "fallback\|CPU" src/swapfase/ui/main_window.py && QT_QPA_PLATFORM=offscreen .venv/bin/python -c "import sys; sys.path.insert(0,'src'); import swapfase.app; from swapfase.providers import select_providers; print(select_providers(prefer_gpu=False))" | grep -q "CPUExecutionProvider" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `src/swapfase/app.py` contains `CPUExecutionProvider` (CPU fallback path) and `QMessageBox` (friendly errors)
    - The app NEVER hard-exits on missing GPU: `grep -c "sys.exit\|raise SystemExit" src/swapfase/app.py` does not appear inside the provider/engine-construction path (CPU path continues); `verify_gpu(raise_on_cpu=True)` is NOT called in app.py (`grep -c "raise_on_cpu=True" src/swapfase/app.py` returns 0)
    - `select_providers(prefer_gpu=False)` returns a list whose only entry is `CPUExecutionProvider` (proves the CPU path is reachable)
    - Camera-busy and no-face-photo both produce a `QMessageBox` (not a traceback): `grep -ci "QMessageBox" src/swapfase/app.py` >= 1
    - `cap.release()` is reachable from a `finally` and from `closeEvent` (always released): `grep -ci "release" src/swapfase/app.py src/swapfase/capture.py | head` shows >= 2 total
    - No frames/photos written to disk in the loop, no network in the frame path: `grep -c "imwrite\|requests.\|urlopen" src/swapfase/app.py src/swapfase/pipeline.py` returns 0
  </acceptance_criteria>
  <done>CPU fallback runs the same pipeline without crashing; the badge shows the real provider; camera-busy and no-face errors are friendly; the camera always releases on stop/crash/close; no network or disk writes in the frame loop.</done>
</task>

<task type="auto">
  <name>Task 2: README — run instructions, GPU-fix note, model acquisition, personal-use/offline note</name>
  <read_first>
    - .planning/phases/01-real-time-webcam-face-swap-mvp/01-SKELETON.md (architectural decisions + run command)
    - .planning/research/STACK.md (§Installation, §Driver fix, §Models)
    - CLAUDE.md (constraints: isolated venv, personal/local, offline)
    - README.md (the file being created)
  </read_first>
  <files>README.md</files>
  <action>
    Create `README.md` documenting how to run and operate the app (the local full-stack run command required by the Walking Skeleton contract):
    - **Run:** `./run.sh` (or `./run.sh --target <photo>`); note it must be launched via `run.sh` so the venv-local CUDA `LD_LIBRARY_PATH` fallback is set (Plan 01).
    - **Setup:** uv venv + the pinned install commands (point at `requirements.txt`); the isolated-venv / never-touch-global constraint.
    - **GPU fix:** the one-time `dkms autoinstall` + `modprobe` driver-module fix (link to D-13 / STACK.md), and that on CPU the app still runs (degraded).
    - **Models:** `buffalo_l` auto-downloads; `inswapper_128(_fp16).onnx` is fetched once from a mirror into `models/` and SHA256-verified; offline thereafter.
    - **Usage:** open app → live preview (swap OFF) → Load photo → Start → mirror/swap toggles, device picker, FPS + GPU/CPU badge.
    - **Privacy/license:** strictly personal/local, nothing published or streamed, runs offline after first model download; inswapper is non-commercial-research licensed (CLAUDE.md).
  </action>
  <verify>
    <automated>cd /home/lexeler/swap-fase && grep -q "run.sh" README.md && grep -qi "inswapper\|models" README.md && grep -qi "dkms\|nvidia" README.md && grep -qi "offline\|personal" README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `run.sh`, a models/inswapper note, a dkms/nvidia GPU-fix note, and an offline/personal-use note
    - The documented run command matches the actual entrypoint (`run.sh` → `run.py` → `swapfase.app.main`)
  </acceptance_criteria>
  <done>README documents setup, the GPU fix, model acquisition, usage, and the personal-use/offline constraints.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final acceptance — full Core-Value experience + degradation + robustness</name>
  <read_first>
    - .planning/research/PITFALLS.md ("Looks Done But Isn't" checklist)
    - .planning/ROADMAP.md (Phase 1 Success Criteria 1–5)
    - .planning/phases/01-real-time-webcam-face-swap-mvp/01-SKELETON.md
  </read_first>
  <files>(no new files — final human acceptance of the whole app)</files>
  <action>
    HUMAN-VERIFY (no code change): Claude launches the finished app with `./run.sh` and the user confirms each ROADMAP Phase 1 success criterion (SC1 GPU badge + nvidia-smi util spike; SC2 live swap + Start/Stop; SC3 smooth + FPS; SC3 CPU survival; SC4 change-photo-without-restart; SC5 no-face passthrough + toggles + friendly no-face error + camera released on close). Exact steps are in <how-to-verify>. Pause for the user's acceptance.
  </action>
  <what-built>
    The complete app: live webcam → largest face swapped onto a loaded photo, shown smoothly in a resizable PySide6 window, with Start/Stop, change-target-without-restart, mirror + swap toggles, device picker, on-screen FPS, a real GPU/CPU badge, CPU graceful fallback, no-face passthrough, friendly camera/photo errors, and guaranteed camera release.
  </what-built>
  <how-to-verify>
    Launch with `./run.sh` and confirm each ROADMAP success criterion:
    1. **GPU verified (SC1):** badge reads "GPU (CUDA)"; `nvidia-smi -l 1` shows a util spike during the loop (not a silent CPU run).
    2. **Live swap end-to-end (SC2):** Load a target photo, press Start — your live face is replaced with the photo's face in the window; Stop ends it and frees the camera.
    3. **Smooth on GPU + FPS (SC3):** the swap feels smooth (target 25–30+ fps) and the on-screen FPS counter reflects it. (If short of 25–30, confirm the D-08 perf knobs were applied — det_size 320 / 640×480 / detect-every-N.)
    4. **CPU survival (SC3):** OPTIONAL — if you can simulate no-GPU (or trust the CPU path), confirm the app still runs degraded with the badge reading "CPU fallback" and no crash.
    5. **Change photo without restart (SC4):** while running, Load a different photo — the swapped face updates live, loop still running, no restart.
    6. **Robust + isolated (SC5):** turn away from the camera → frame passes through unchanged (no crash); toggle mirror and swap on/off → both work; load a photo with no face → friendly message, previous target kept; close the app and re-launch → camera reopens (it was released).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all six checks above pass (GPU badge + util spike, live swap, smooth+FPS, change-target-live, no-face passthrough, toggles, friendly no-face error, camera released on close)
    - The provider badge matches reality (`nvidia-smi` util present ⇔ badge says GPU)
    - No crash across the full session; the camera is reusable after close
  </acceptance_criteria>
  <resume-signal>Type "phase accepted" if all six success criteria pass, or list which check failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPU availability → app behavior | The app must survive (degrade) when CUDA is absent rather than fail. |
| local devices/files → friendly error surface | Camera-busy and bad-photo conditions must not crash or leak tracebacks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19 | Denial of Service | missing/unhealthy GPU crashes the app | mitigate | CPU fallback runs the same pipeline; app never hard-exits on missing GPU; badge reads "CPU fallback" (LIVE-03, D-17). |
| T-01-20 | Spoofing | badge lies about the provider (claims GPU while on CPU) | mitigate | Badge sources `engine.provider` from a real warm-up inference (`active_provider`), not the requested list (Pitfall 3, UI-05). |
| T-01-21 | Denial of Service | camera handle leaked on crash → device unusable until reboot | mitigate | `cap.release()` in finally + closeEvent + on exception; re-launch reopens (Pitfall 10). |
| T-01-22 | Denial of Service | faceless frame / bad photo crashes the loop or app | mitigate | No-face passthrough (D-18, SWAP-03); bad-photo → friendly QMessageBox keeping the previous target. |
| T-01-23 | Information Disclosure | live face frames leaving the machine or persisted | mitigate | No network in the frame path; no frame/photo disk writes; offline after the one-time model download (privacy constraint). |
</threat_model>

<verification>
- GPU unavailable → app degrades to a working CPU path (same pipeline, no crash); badge shows the real provider; FPS reflects actual speed.
- No-face frame passes through unchanged live; camera-busy and no-face-photo show friendly messages.
- Camera always released on Stop/crash/close; re-launch reopens it.
- README documents run/setup/GPU-fix/models/usage/privacy.
- Human accepts the full Core-Value experience against ROADMAP SC1–SC5.
</verification>

<success_criteria>
- LIVE-03: smooth on GPU; degraded-but-functional CPU fallback through the same pipeline (no crash).
- SWAP-03: no-face frames pass through unchanged without crashing (confirmed live).
- UI-05: GPU/CPU provider badge shows the REAL active provider.
</success_criteria>

<output>
After completion, create `.planning/phases/01-real-time-webcam-face-swap-mvp/01-05-SUMMARY.md` recording: the final GPU FPS and (if tested) CPU FPS, confirmation of the badge honesty + no-face passthrough + friendly errors + camera release, the human acceptance result against ROADMAP SC1–SC5, and any D-08 perf knobs that were needed to hit 25–30 fps.
</output>
